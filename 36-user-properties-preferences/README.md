# 36. Per-User Settings with User Properties

**What it does:** Stores preferences per user (each user gets their own values).

**When to use it:** Multi-user scripts with personal settings.

## How to run
Run `setUserPref()` then `getUserPref()`.

## Code
See: [`Code.gs`](./Code.gs)
