# 38. Create a Time-Driven Trigger

**What it does:** Creates an hourly trigger for `hourlyJob`.

**When to use it:** Scheduled automations.

## How to run
Run `createHourlyTrigger()` once. Then check Triggers in Apps Script UI.

## Code
See: [`Code.gs`](./Code.gs)
