# 30. Move a File to a Folder

**What it does:** Moves a file into a folder (and optionally removes from root).

**When to use it:** Automated Drive organization.

## How to run
Call `moveFileToFolder(FILE_ID, FOLDER_ID)`. Requires Drive access.

## Code
See: [`Code.gs`](./Code.gs)
