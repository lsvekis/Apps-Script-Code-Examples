# 46. Read a Named Range

**What it does:** Reads values from named range `MyRange`.

**When to use it:** Template-driven sheets.

## How to run
Run after creating `MyRange` (see snippet 45).

## Code
See: [`Code.gs`](./Code.gs)
