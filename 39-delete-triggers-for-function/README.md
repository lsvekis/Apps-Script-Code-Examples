# 39. Delete All Triggers for a Function

**What it does:** Deletes triggers that call a specific handler function.

**When to use it:** Reset/cleanup trigger setups.

## How to run
Run `demoDeleteHourly()` to remove triggers for `hourlyJob`.

## Code
See: [`Code.gs`](./Code.gs)
