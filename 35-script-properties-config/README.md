# 35. Store a Setting in Script Properties

**What it does:** Saves and retrieves script-level configuration values.

**When to use it:** Avoid hardcoding API keys/settings.

## How to run
Run `setConfig()` first, then `getConfig()`.

## Code
See: [`Code.gs`](./Code.gs)
