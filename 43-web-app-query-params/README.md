# 43. Parse Query Params in doGet

**What it does:** Reads `?name=` from URL and responds with HTML.

**When to use it:** Personalized responses via querystring.

## How to run
Deploy as Web App and visit: `.../exec?name=Lars`.

## Code
See: [`Code.gs`](./Code.gs)
