# 40. Log to a “Logs” Sheet

**What it does:** Appends timestamped log messages into a 'Logs' sheet.

**When to use it:** Debugging in environments where Logger isn't enough.

## How to run
Run `demoLog()` in a Sheets-bound script.

## Code
See: [`Code.gs`](./Code.gs)
