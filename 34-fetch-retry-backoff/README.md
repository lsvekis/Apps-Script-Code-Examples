# 34. Retry Fetch with Backoff

**What it does:** Retries a fetch up to 3 times with increasing delays.

**When to use it:** APIs that sometimes time out or fail.

## How to run
Run `demoRetry()`. Adjust URL. Requires external fetch permissions.

## Code
See: [`Code.gs`](./Code.gs)
