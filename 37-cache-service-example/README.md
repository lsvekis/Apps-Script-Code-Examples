# 37. Cache Expensive Results (CacheService)

**What it does:** Caches a value for 5 minutes to speed repeated calls.

**When to use it:** Avoid recomputing expensive results.

## How to run
Run `Logger.log(cachedHello())` twice to see caching behavior.

## Code
See: [`Code.gs`](./Code.gs)
