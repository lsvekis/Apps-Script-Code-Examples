# 41. Create a Simple Web App (doGet)

**What it does:** Returns basic HTML when deployed as a web app.

**When to use it:** Simple web pages/endpoints.

## How to run
Deploy as Web App: `Deploy` → `New deployment` → `Web app`.

## Code
See: [`Code.gs`](./Code.gs)
