# 15. Create a New Sheet if Missing

**What it does:** Ensures a sheet exists (create if missing).

**When to use it:** Scripts that depend on predictable tabs.

## How to run
Run `demoEnsureSheet()` in a Sheets-bound script.

## Code
See: [`Code.gs`](./Code.gs)
