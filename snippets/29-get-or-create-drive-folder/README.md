# 29. Create a Drive Folder (If Missing)

**What it does:** Gets a folder by name, or creates it if missing.

**When to use it:** Organizing outputs.

## How to run
Run `demoFolder()`. Requires Drive access.

## Code
See: [`Code.gs`](./Code.gs)
